# CyberWatch Newsletter -- 2026-09-25

5 items out of 608 considered, known-exploited first, then severity and reach (cap 5).

Selection: 5 known-exploited (in the CISA KEV catalogue, attacks are happening now). Each item says which under "Why this is here".

---

## 1. KNOWN EXPLOITED -- [CVE-2025-39682](https://nvd.nist.gov/vuln/detail/CVE-2025-39682)  
`KNOWN EXPLOITED` `CRITICAL` `CVSS 9.8` `published 2025-09-05`

> **KNOWN EXPLOITED.** CISA lists this as known-exploited in Linux Kernel on 2026-09-18, so it is being used against people now -- that outranks severity.

**What happened:** A critical flaw in the Linux kernel's TLS implementation mishandles zero-length records on the receive list, potentially causing processing errors during message reception. This issue arises when an initial record from the receive list is zero-length, bypassing expected type-checking logic.

**Who is affected:** Users running the Linux kernel with TLS enabled are affected, including systems using Debian Linux and Siemens SIMATIC CN 4100 firmware.

**How serious:** The vulnerability is rated Critical with a CVSS score of 9.8, indicating it allows for complete compromise of confidentiality, integrity, and availability.

**What to do:** Apply the stable kernel patches linked in the references to resolve the TLS record handling issue.

**Why this is here:** Known exploited -- CISA lists this as known-exploited in Linux Kernel on 2026-09-18, so it is being used against people now -- that outranks severity. CRITICAL (CVSS 9.8); reach 100/100 via "linux kernel".

---

## 2. KNOWN EXPLOITED -- [CVE-2026-94127](https://nvd.nist.gov/vuln/detail/CVE-2026-94127)  
`KNOWN EXPLOITED` `CRITICAL` `CVSS 9.3` `published 2026-09-22`

> **KNOWN EXPLOITED.** CISA lists this as known-exploited in F5 BIG-IP APM on 2026-09-22, so it is being used against people now -- that outranks severity.

**What happened:** A vulnerability in F5 BIG-IP APM allows unauthenticated remote code execution when the system is configured as an OAuth Authorization Server. The flaw exists in the data plane and does not affect the control plane.

**Who is affected:** Organizations running F5 BIG-IP with APM configured specifically as an OAuth Authorization Server are at risk. Deployments using APM strictly as an OAuth Client or Resource Server without authorization server profiles are not affected.

**How serious:** This is a critical vulnerability with a CVSS score of 9.3 that enables remote code execution by unauthenticated attackers. The impact is severe as it allows full system compromise on the data plane.

**What to do:** Review your BIG-IP configuration to determine if the OAuth Authorization Server profile is enabled. Apply the vendor-recommended patch or mitigation steps outlined in the official security advisory.

**Why this is here:** Known exploited -- CISA lists this as known-exploited in F5 BIG-IP APM on 2026-09-22, so it is being used against people now -- that outranks severity. CRITICAL (CVSS 9.3); reach 56/100 via "big-ip".

---

## 3. KNOWN EXPLOITED -- [CVE-2026-5430](https://nvd.nist.gov/vuln/detail/CVE-2026-5430)  
`KNOWN EXPLOITED` `CRITICAL` `CVSS 10.0` `published 2026-08-06`

> **KNOWN EXPLOITED.** CISA lists this as known-exploited in WSO2 Multiple Products on 2026-09-24, so it is being used against people now -- that outranks severity.

**What happened:** The JWT authentication mechanism incorrectly accepts tokens signed with unsupported algorithms, allowing attackers to bypass validation and gain unauthorized access. This flaw can lead to full account takeover and compromise of administrative accounts.

**Who is affected:** Users of WSO2 Universal Gateway, Traffic Manager, API Control Plane, API Manager, and Carbon API Manager Rest API Utility are at risk.

**How serious:** The vulnerability is rated Critical with a CVSS score of 10.0, indicating that exploitation can result in complete system compromise without authentication.

**What to do:** Review the official WSO2 security advisory for details on mitigation and patching steps to address this authentication bypass flaw.

**Why this is here:** Known exploited -- CISA lists this as known-exploited in WSO2 Multiple Products on 2026-09-24, so it is being used against people now -- that outranks severity. CRITICAL (CVSS 10.0); reach unrated (no entry in the reach table).

---

## 4. KNOWN EXPLOITED -- [CVE-2026-93616](https://nvd.nist.gov/vuln/detail/CVE-2026-93616)  
`KNOWN EXPLOITED` `CRITICAL` `CVSS 9.8` `published 2026-09-22`

> **KNOWN EXPLOITED.** CISA lists this as known-exploited in Check Point Multiple Products on 2026-09-22, so it is being used against people now -- that outranks severity.

**What happened:** An unauthenticated attacker can exploit a directory traversal and file upload flaw to upload and execute arbitrary scripts on the affected Check Point Management Server.

**Who is affected:** Users of Check Point Quantum Security Management, multi-domain security management, and quantum security management products are at risk.

**How serious:** The vulnerability is rated Critical with a CVSS score of 9.8, indicating a high likelihood of complete system compromise.

**What to do:** Administrators should consult the Check Point support advisory and CISA catalog for immediate mitigation steps and patch information.

**Why this is here:** Known exploited -- CISA lists this as known-exploited in Check Point Multiple Products on 2026-09-22, so it is being used against people now -- that outranks severity. CRITICAL (CVSS 9.8); reach unrated (no entry in the reach table).

---

## 5. KNOWN EXPLOITED -- [CVE-2026-93952](https://nvd.nist.gov/vuln/detail/CVE-2026-93952)  
`KNOWN EXPLOITED` `CRITICAL` `CVSS 9.5` `published 2026-09-22`

> **KNOWN EXPLOITED.** CISA lists this as known-exploited in Arista VeloCloud Orchestrator on 2026-09-22, so it is being used against people now -- that outranks severity.

**What happened:** A vulnerability in VeloCloud Orchestrator on-prem allows remote attackers to access privileged internal functionality and impact the host. Successful exploitation can compromise the confidentiality, integrity, and availability of the orchestrator and its managed data.

**Who is affected:** Organizations running Arista Networks VeloCloud Orchestrator on-prem, including hosted and dedicated versions, are affected by this issue.

**How serious:** The vulnerability is rated as critical with a CVSS score of 9.5, indicating severe potential impact on system security and data.

**What to do:** Administrators should apply the available patches to their VeloCloud Orchestrator instances immediately to mitigate the risk of exploitation.

**Why this is here:** Known exploited -- CISA lists this as known-exploited in Arista VeloCloud Orchestrator on 2026-09-22, so it is being used against people now -- that outranks severity. CRITICAL (CVSS 9.5); reach unrated (no entry in the reach table).

---

_Summaries: 5/5 written by a local LLM._
_Source: NVD CVE 2.0 API._
